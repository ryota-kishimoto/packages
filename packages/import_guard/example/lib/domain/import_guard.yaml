# Domain layer import_guard.yaml
# Clean Architecture: Domain layer should not depend on Presenter layer.

deny:
  # Deny all imports from presenter layer
  - package:import_guard_example/presenter/**
